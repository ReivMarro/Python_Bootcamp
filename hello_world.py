print ("Hello world from Python 101")

# When using and checking work on Python, need to run Terminal (cmd or Terminal in VSC)
    # Then need to type cd desktop, and follow path to the folder, then type dir to find files/items
        # Each Python file to run will end with .py